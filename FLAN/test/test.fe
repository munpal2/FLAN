$ 다양한 기능의 테스트용 파일
func add(int a, int b): ptr of uint
{
	return NULL;
}

for (decl int i; i < 5; i++)
{
} $ 이런ㄴ것도 되나?

decl const uint a = 0, b, c;
decl arr(5U) of arr(10U) of int foo;

foo[a] += 3;
if (foo[0] != 0)
{
	foo[4 * 3] += 4 * 8;
	foo[add(1, 2)][1] = add(100, 2000);
} 